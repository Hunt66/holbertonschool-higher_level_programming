#!/usr/bin/python3

#This program generates a randome number and prints the number followed
#by is positive is zero or is negative depending on which it is.


import random
number = random.randint(-10, 10)
if number > 0:
    print("{:d} is positive" .format(number))
elif number < 0:
    print("{:d} is negative" .format(number))
else:
    print("{:d} is zero" .format(number))
