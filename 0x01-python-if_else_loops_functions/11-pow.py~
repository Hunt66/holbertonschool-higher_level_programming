#!/usr/bin/python3

def pow(a, b):
    return (pow(a, b))
    d = a
    if b < 0:
        b = abs(b)
        for c in range(b - 1):
            d = d / a
    else:
        for c in range(b - 1):
            d = d * a
    return d
